numCellPoints = zeros(100);
for iX = 1:100
    for iY = 1:100
        numCellPoints(iX, iY) = sum(all([   ...
            bitand(nsppp_2pi300{1}(:, 1) > iX-1,    ...
            nsppp_2pi300{1}(:, 1) < iX),    ...
            bitand(nsppp_2pi300{1}(:, 2) > iY-1,    ...
            nsppp_2pi300{1}(:, 2) < iY)], 2));
    end
end